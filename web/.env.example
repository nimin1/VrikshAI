# VrikshAI Web App Environment Variables

# API Configuration
# For Vercel monorepo deployment (backend and frontend together):
# Use relative path - /api
REACT_APP_API_URL=/api

# Note: For local full-stack development, the proxy in package.json
# automatically forwards /api/* to http://localhost:5001
# No need to change this setting for local development.
