{
  "buildCommand": "cd web && npm install && npm run build",
  "outputDirectory": "web/build",
  "installCommand": "cd web && npm install",
  "devCommand": "cd web && npm start",
  "framework": "create-react-app",
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/api/$1"
    },
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
